# Database connection string
DATABASE_URL=postgresql://user:password@localhost:5432/shoreline

# Mapbox access token for the beach map
MAPBOX_ACCESS_TOKEN=pk.your_mapbox_token

# Optional: server port
PORT=3000
